- var page = 'rule'

doctype html
html(lang="zh-CN")

  head
    meta(charset="utf-8")
    meta(name="viewport" content="width=device-width, initial-scale=1")

    include ../inc/ogp
    +ogp('电脑麻将: 规则设置', '可在对局中使用自定义规则')

    include ../inc/style
    include ../inc/script

  body.rule
    include ../inc/loaderr
    include ../inc/navi
    .version ver. #{version}

    include ../inc/rule
